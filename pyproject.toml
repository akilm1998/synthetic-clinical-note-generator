[project]
name = "synthetic-clinical-note-generator"
version = "0.1"

description = "Generate synthetic clinical notes using LLMs."
requires-python = ">=3.13"

dependencies = [
    "pyyaml",
    "ruff",
    "openai",
    "python-dotenv",
    "pandas",
    "pyarrow",
    "requests",
    "beautifulsoup4",
]